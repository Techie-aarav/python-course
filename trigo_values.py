def calculate_trig_values():
    import math
    x = int(input("Please enter the angle value: "))
    a = str(input("Please enter the trignometric ratio for which you want to find the angle value"))
    sin_value = math.sin(angle_radians)
    cos_value = math.cos(angle_radians)
    tan_value = math.tan(angle_radians)
print(f"sin({angle}) = {sin_val}")
print(f"cos({angle}) = {cos_val}")
print(f"tan({angle}) = {tan_val}")

    